name: Cachy Kernel test
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
#    secrets: inherit
#    permissions:
#      contents: read
#      packages: write
    uses: ./.github/workflows/reusable-action.yml
    with:
      image-name: cachykernel
      containerfile: test/Containerfile-cachykernel
      tag: latest
      space: false
      rechunk: true
      push: true
  #    sign: true
